label ch06Gomoku(forcePlay = False):

    if builditTesting and (not forcePlay):
        return

    # 윷놀이 전파는 대차게 실패
    # 실력만으로 승부할 수 있는 게임을 전파해 보자
    # 오목, 바둑, 장기 소개
    # 이 장면 이후 로잘린드의 호감도와 관계없이 '몽정' 신 감상 가능

    return

