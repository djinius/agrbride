label charaEp06Backdraft(forcePlay = False):

    if builditTesting and (not forcePlay):
        return

    # 분뇨의 역류
    # 마천루 건설 이후
    # 하수구 막힌 곳에서 원인을 파악하다가 똥에 파묻힌 주인공
    # 카라가 똥을 죄다 먹어치워 구해줌
    # 그 과정에서 주인공의 옷을 죄다 녹여버림 - 똥물이 배서 어쩔 수 없음
    # 주인공이 사타구니를 손으로 가리자, 이미 다 봤는데 굳이 가리냐는 카라
    # 주인공은 카라에게 진심으로 감사함
    # 주인공을 삐딱하게 보던 카라였지만, 진심어린 감사에 마음을 엶
    # 선택지: 키스한다 - 에피소드 3/3-1을 본 상태
    # 선택지: 옷을 달라고 한다 - 항상
    # 키스한다 선택시 H신이 이어짐
    # 옷을 달라고 한다 선택시 감사 인사 후 헤어짐

    return

label charaFirstSex(forcePlay = False):

    if builditTesting and (not forcePlay):
        return

    return
